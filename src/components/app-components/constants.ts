import { cssClases } from "./cssclases";

export const LOGIN_LINK: HTMLElement | null = document.querySelector(`.${cssClases.loginBtn}`);
export const REGISTR_LINK: HTMLElement | null = document.querySelector(`.${cssClases.registrBtn}`);
export const MY_PROFILE_LINK: HTMLElement | null = document.querySelector(`.${cssClases.myProfile}`);
export const LOGAUT_BTN: HTMLElement | null = document.querySelector(`.${cssClases.logautBtn}`);
export const PASS_INPUT: HTMLElement | null = document.getElementById('password');

